import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { BubrahPage } from './bubrah';

@NgModule({
  declarations: [
    BubrahPage,
  ],
  imports: [
    IonicPageModule.forChild(BubrahPage),
  ],
})
export class BubrahPageModule {}
